<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_token_wallet_line_tree" model="ir.ui.view">
        <field name="name">token.wallet.line.list</field>
        <field name="model">token.wallet.line</field>
        <field name="arch" type="xml">
            <list>
                <field name="partner_id"/>
                <field name="product_tmpl_id"/>
                <field name="product_id"/>
                <field name="token_count"/>
            </list>
        </field>
    </record>

    <record id="view_token_wallet_line_search" model="ir.ui.view">
        <field name="name">token.wallet.line.search</field>
        <field name="model">token.wallet.line</field>
        <field name="arch" type="xml">
            <search>
                <field name="partner_id"/>
                <field name="product_tmpl_id"/>
                <field name="product_id"/>
                <group expand="0" string="Group By">
                    <filter string="Partner" name="group_partner" context="{'group_by': 'partner_id'}"/>
                    <filter string="Product" name="group_product" context="{'group_by': 'product_tmpl_id'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="action_token_wallet_lines" model="ir.actions.act_window">
        <field name="name">Token Wallet Lines</field>
        <field name="res_model">token.wallet.line</field>
        <field name="view_mode">list</field>
        <field name="search_view_id" ref="view_token_wallet_line_search"/>
    </record>

    <menuitem id="menu_token_wallet_root" name="Token Wallet" parent="contacts.menu_contacts" sequence="90"/>
    <menuitem id="menu_token_wallet_lines" name="Ledger" parent="menu_token_wallet_root" action="action_token_wallet_lines" sequence="10"/>
</odoo>
